# The @deepseek-ai seam packages are still pre-release on npm and reference
# unpublished siblings, so the registry cannot resolve them yet. The plugin's
# peers are provided at runtime by the harness; for local build/test they are
# linked from a harness checkout by scripts/link-dev-deps.sh.
auto-install-peers=false
